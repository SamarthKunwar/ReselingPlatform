spring.application.name=backend

# Spring Data JPA configuration



spring.datasource.url=${DB_URL:jdbc:mysql://127.0.0.1:3306/resell}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD}

# JPA/Hibernate specific configurations (optional, adjust as needed)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true




# JWT Configuration
# Default secret is a sample 256-bit key for local dev. In prod, set JWT_SECRET env var.
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}


